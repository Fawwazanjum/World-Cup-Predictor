__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.eggs/

# processed outputs (generated files, not source)
data/processed/*
!data/processed/.gitkeep
!data/processed/wc2026_predictions.csv

# trained model artefacts — kept in repo so simulation can run without retraining
# models/*.pkl  (intentionally not excluded)

# jupyter
.ipynb_checkpoints/
